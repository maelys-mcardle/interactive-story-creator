var history = [];

function appendHistory( history, day, time, location, choiceId )
{
    history.push({
        day: day,
        time: time,
        location: location,
        choiceId: choiceId });
        
    return history;
}

function showStoryPage( story, linkText )
{
    
}
